const CountdownController = (() => {

    return {
        init(){
            (function initVM () {

            })();

            (function initDOM () {
                CountdownDirective.init();
            })();
        }
    };
})();
