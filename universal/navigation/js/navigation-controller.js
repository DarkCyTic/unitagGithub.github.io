const NavigationController = (() => {

    return {
        init(){
            (function initVM () {

            })();

            (function initDOM () {
                NavigationDirective.init();
            })();
        }
    };
})();
